import os

def download_models():
    print("Downloading Whisper models...")
    # Placeholder for actual download logic
    # e.g. using whisper library to load model which caches it
    print("Models downloaded successfully.")

if __name__ == "__main__":
    download_models()
